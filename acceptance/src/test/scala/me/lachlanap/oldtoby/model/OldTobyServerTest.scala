package me.lachlanap.oldtoby.model

import scala.util.Success

class OldTobyServerTest extends UnitSpec {

  import me.lachlanap.oldtoby.model.Helpers._

  "Server API" should "have no jobs to start with" in {
    fail("not implemented")
  }


  ignore can "create a stage definition" in {}


  ignore can "create a pipeline" in {}


  ignore can "create a region" in {}

  ignore can "create a job" in {}

  ignore can "schedule a batch of work for a region" in {}
  ignore can "schedule a batch of work for a stage" in {}
  ignore can "get a scheduled batch of work" in {}
}
